{
  "name": "salon-swift-booking-system",
  "version": "1.0.0",
  "description": "Complete salon booking system with Google Sheets integration",
  "scripts": {
    "build": "npm run build",
    "install": "npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
